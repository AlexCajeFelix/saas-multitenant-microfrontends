# Gateway do stack local (docker compose). Bate com GATEWAY_PORT do .env da raiz.
VITE_API_URL=http://localhost:8000

# Chave anon gerada por `make keys`. Rode `make web-env` na raiz para copiar
# a chave do .env para o web/.env.local sem precisar abrir os dois arquivos.
VITE_ANON_KEY=
